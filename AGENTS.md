# 项目工作规则

本文件适用于整个仓库，开发者和自动化代理都必须遵守。

## 开始任务前

1. 阅读根目录 `README.md`、`docs/README.md` 和相关分类文档。
2. 查看 `docs/tasks/README.md` 中最近的任务记录，避免重复或违反既有决定。
3. 为本次任务创建 `docs/tasks/YYYY-MM-DD-NNN-任务名.md`，状态先写“进行中”。

## 实施过程中

1. 规则、架构、平台或资产约定发生长期变化时，更新对应分类文档。
2. 新建、删除或移动任何工程文件时，同一任务必须更新根 README 的工程目录。
3. 新增、删除或重命名文档时，更新 `docs/README.md` 和所在分类 README。
4. 不提交 `.tools/`、`.packages/`、`.godot/`、`artifacts/`、日志、存档或编辑器私有状态。

## 复杂度约束

1. 不增加没有当前需求支撑的抽象层、重复校验、状态哈希、镜像状态或多套恢复路径。
2. 同一业务规则只能有一个权威实现，其他模块通过接口复用结果。
3. 测试聚焦规则边界、状态转换和已知风险，不以重复断言或无依据的防御代码替代清晰设计。
4. 保持高内聚低耦合：规则层不依赖 Godot，表现层不重新实现规则，玩法模块不越界读写其他模块内部状态。
5. 安全、存档迁移或明确数据损坏风险确需额外校验时，任务档案必须写明故障模型、收益和验证方式。

## 完成任务前

1. 补齐任务记录中的实际改动、验证结果和遗留事项，并把状态改为“已完成”或“已阻塞”。
2. 更新 `docs/tasks/README.md` 索引。
3. 运行 `./scripts/verify.sh`。
4. 检查 `git status --ignored` 和 `.gitignore`，确认没有缓存、构建产物或本地敏感文件被提交。

未完成任务留档、文档索引或工程目录同步的改动不允许提交。
